<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.3.xsd">
	<http pattern="/resources/**" security="none"></http>
	<http auto-config="true" use-expressions="true">

		<intercept-url pattern="/main" access="permitAll" />
		<intercept-url pattern="/idCheck" access="permitAll" />
		<intercept-url pattern="/regiMail" access="permitAll" />
		<intercept-url pattern="/register" access="permitAll" />
		<intercept-url pattern="/regiMail_check" access="permitAll" />
		
		<!-- 공지사항 관련(권한 미설정) -->
		<intercept-url pattern="/noticeList" access="permitAll"/>
		<intercept-url pattern="/noticeWritePage" access="permitAll"/>
		<intercept-url pattern="/noticeWrite" access="permitAll"/>
		<intercept-url pattern="/noticeContentPage" access="permitAll"/>
		<intercept-url pattern="/deleteNotice" access="permitAll"/>
		<intercept-url pattern="/updateNoticePage" access="permitAll"/>
		<intercept-url pattern="/updateNoitce" access="permitAll"/>
		
		
		<intercept-url pattern="/admin/**" access="hasRole('ROLE_ADMIN')" />
		<intercept-url pattern="/*"
			access="hasAnyRole('ROLE_ADMIN', 'ROLE_USER')" />
	
		<form-login 
		username-parameter="id" 
		password-parameter="pw"
			login-page="/main" 
			authentication-failure-url="/main?error=true"
			authentication-success-handler-ref="userLoginSuccessHandler" />

		<logout logout-url="/logout"
				logout-success-url="/main" 
				invalidate-session="true" />

		<access-denied-handler error-page="/denied" />
		
		<session-management invalid-session-url="/main">
			<concurrency-control max-sessions="1"
				expired-url="/main" />
		</session-management>
	</http>

	<beans:bean id="userLoginSuccessHandler"
		class="com.crossit.hcc.service.UserLoginSuccessHandler"></beans:bean>

	<authentication-manager>
		<authentication-provider user-service-ref="UserLoad" />
	</authentication-manager>



</beans:beans>
